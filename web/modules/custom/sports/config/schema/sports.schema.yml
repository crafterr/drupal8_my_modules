sports.settings:
  type: config_object
  label: 'Sports Page Settings'
  mapping:
    page_limit_for_team:
      type: integer
      label: 'Per page item limit for Team'
    page_limit_for_player:
      type: integer
      label: 'Per page item limit for Player'

views.filter.team_filter:
  type: views_filter
  label: 'The teams to filter by'
  mapping:
    value:
      type: sequence
      label: 'Teams'
      sequence:
        type: string
        label: 'Team'